VITE_API_BASE_URL=http://localhost:8000

# --- Deploying to Render ---
# VITE_API_BASE_URL: the deployed backend's URL (e.g.
#   https://beyondtheveil-backend.onrender.com). Vite bakes this in at
#   build time, so it must be set in Render's build environment, not just
#   at runtime.
